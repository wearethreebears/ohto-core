import { $system } from "~/DesignTokens.config";
import type { TTRStaticTokens } from "./TR.types";

export const STATIC_TOKENS: TTRStaticTokens = {
  borderColor: $system.borders.colors.$default,
};
